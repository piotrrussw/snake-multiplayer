{
  "name": "snake-multiplayer",
  "version": "1.0.0",
  "description": "multiplayer snake project written in TypeScript based on WebSockets (socket.io)",
  "scripts": {
    "dev": "tsc"
  },
  "devDependencies": {
    "@types/p5": "^0.7.1",
    "typescript": "^2.9.2"
  }
}
